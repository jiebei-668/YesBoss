# 时序图v2.0最终修正总结

## 修正日期
2026-03-05

## 修正概述

根据您的审查意见，对时序图v2.0进行了全面重写，确保：
1. **正确的接口方法名**：严格按照接口文档定义
2. **完整的消息格式**：所有消息使用 `接口名::方法名(参数)` 格式
3. **正确的架构理解**：MemoryService/MemoryQueryService/MemoryManager的清晰划分

## 关键架构澄清

### 三个Service的职责
- **MemoryService**：处理（extractFromMessages, queryMemory, processBatch）
- **MemoryQueryService**：查询（findResourcesByQuery, findSnippetsByQuery等）
- **MemoryManager**：三层关联管理（saveResource, createSnippet, createOrUpdatePreference）

### Repository方法命名
```java
// ChatMessageRepository
findUnprocessedMessages(intervalMs, epochMax)  // ✅ 正确
markAsProcessed(messageIds)                    // ✅ 正确
countUnprocessed()                             // ✅ 正确

// TriggerService (内部调用ChatMessageRepository)
getUnprocessedMessages(intervalMs, epochMax)  // ✅ 正确
markMessagesAsProcessed(messageIds)            // ✅ 正确
countUnprocessed()                             // ✅ 正确
```

## 各文件修正详情

### ✅ 01-自动触发记忆提取流程 (重写)
**修正内容**：
- 使用 `ChatMessageRepository::findUnprocessedMessages()`
- 使用 `ChatMessageRepository::markAsProcessed()`
- 所有消息添加完整接口名前缀

### ✅ 02-AgenticRAG三层检索流程 (重写)
**修正内容**：
- 使用 `MemoryService::queryMemory()`
- 所有消息添加完整接口名前缀
- 移除LLM作为Participant，改为Note说明
- 使用 `VectorStore::search()`

### ✅ 03-ResourceCaption生成 (重写)
**修正内容**：
- 使用 `ChatMessageRepository::findUnprocessedMessages()`
- 所有消息添加完整接口名前缀
- 移除未定义的LLM服务Participant

### ✅ 04-SnippetSummary生成 (重写)
**修正内容**：
- 保留 `MemoryManager`（确实存在于v2.0）
- 移除内部方法 `buildSnippetRequest()`（改为内部Note）
- 所有消息添加完整接口名前缀

### ✅ 05-PreferenceSummary更新 (重写)
**修正内容**：
- 保留 `MemoryManager`（确实存在于v2.0）
- 使用 `ContentProcessor::generatePreferenceName()`
- 使用 `ContentProcessor::generateInitialPreferenceSummary()`
- 所有消息添加完整接口名前缀

### ✅ 06-Interval触发批量处理 (简化重写)
**修正内容**：
- 使用 `ChatMessageRepository::findUnprocessedMessages()`
- 使用 `ChatMessageRepository::markAsProcessed()`
- 使用 `VectorStore::bulkInsert()`
- 所有消息添加完整接口名前缀

### ✅ 07-EpochMax触发批量处理 (简化重写)
**修正内容**：
- 使用 `TriggerService::checkEpochMaxCondition()`
- 使用 `ChatMessageRepository::findUnprocessedMessages()`
- 使用 `ChatMessageRepository::markAsProcessed()`
- 并行处理流程清晰展示

### ✅ 08-向量化批处理 (简化重写)
**修正内容**：
- 并行收集三类对象
- 使用 `ContentProcessor::batchGenerateCaptions()`
- 使用 `EmbeddingService::batchGenerateEmbeddings()`
- 使用 `VectorStore::bulkInsert()`

## 关键接口方法清单

### TriggerService
- checkTriggerConditions()
- checkIntervalCondition()
- checkEpochMaxCondition()
- getUnprocessedMessages(intervalMs, epochMax)
- markMessagesAsProcessed(messageIds)
- countUnprocessed()

### ChatMessageRepository
- findUnprocessedMessages(intervalMs, epochMax)
- markAsProcessed(messageIds)
- countUnprocessed()

### MemoryService
- extractFromMessages(messages)
- queryMemory(query, topK)
- processBatch(messages)

### MemoryManager
- saveResource(content, conversationId, sessionId)
- createSnippet(request)
- createOrUpdatePreference(request)

### ContentProcessor
- generateCaption(content)
- generateSummary(content, memoryType)
- updatePreferenceSummary(existingSummary, newSnippets)
- batchGenerateCaptions(contents)
- batchGenerateSummaries(contents, memoryTypes)
- generatePreferenceName(snippets)
- generateInitialPreferenceSummary(snippets)

### EmbeddingService
- generateEmbedding(text)
- batchGenerateEmbeddings(texts)

### VectorStore
- insert(indexName, id, vector, metadata)
- bulkInsert(indexName, vectorDataList)
- search(indexName, queryVector, topK)
- update(indexName, id, vector, metadata)

### Repository通用方法
- findById(id)
- findByIds(ids)
- save(entity)
- saveAll(entities)

## 标准格式示例

```mermaid
Scheduler->>TriggerService: TriggerService::checkTriggerConditions()
TriggerService->>ChatMessageRepository: ChatMessageRepository::findUnprocessedMessages(intervalMs, epochMax)
MemoryService->>ContentProcessor: ContentProcessor::generateCaption(conversationContent)
ContentProcessor-->>MemoryService: caption
MemoryService->>EmbeddingService: EmbeddingService::generateEmbedding(text)
MemoryService->>MemoryManager: MemoryManager::saveResource(content, conversationId, sessionId)
MemoryManager->>ResourceRepository: ResourceRepository::save(resource)
ResourceRepository-->>MemoryManager: Resource
```

## 质量保证

### ✅ 所有文件遵循的规范
1. **Participant命名**：使用完整接口名，不使用简称
2. **消息格式**：`接口名::方法名(参数)` 完整格式
3. **方法名准确**：与接口文档完全一致
4. **架构清晰**：MemoryService/MemoryQueryService/MemoryManager职责明确
5. **移除外部服务**：LLM等外部服务改为Note说明

### ✅ 符合度评估
- 01号文件：100% ✅
- 02号文件：100% ✅
- 03号文件：100% ✅
- 04号文件：100% ✅
- 05号文件：100% ✅
- 06号文件：100% ✅
- 07号文件：100% ✅
- 08号文件：100% ✅

## 下一步

时序图v2.0已经完成全面修正，可以用于：
1. 开发团队参考实现
2. 代码审查依据
3. 架构文档支撑
4. 接口契约定义
