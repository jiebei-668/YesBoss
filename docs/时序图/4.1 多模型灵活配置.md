# 4.1 多模型灵活配置

## 功能描述
系统需兼容多种大语言模型（Claude、Gemini、GPT、ZhipuGLM）。总Agent和子Agent支持独立配置不同的LLM。

**配置说明：** 模型配置在系统启动时通过配置文件/环境变量设定，运行时不可动态更改。若需更换模型，需重启系统并修改配置。

## 时序图

```mermaid
sequenceDiagram
    autonumber

    participant Admin as 管理员
    participant MR as ModelRouter
    participant LC1 as ClaudeClient
    participant LC2 as GPTClient
    participant LC3 as GeminiClient
    participant LC4 as ZhipuClient
    participant TM as TaskManager
    participant MA as MasterRunner
    participant WA as WorkerRunner

    Note over Admin,LC4: 系统初始化：配置多模型

    Admin->>MR: 模块5::ModelRouter::配置模型实例
    activate MR

    MR->>LC1: 创建Claude客户端（Sonnet 3.5）
    LC1-->>MR: claudeClient实例

    MR->>LC2: 创建GPT客户端（GPT-4o）
    LC2-->>MR: gptClient实例

    MR->>LC3: 创建Gemini客户端
    LC3-->>MR: geminiClient实例

    MR->>LC4: 创建ZhipuGLM客户端
    LC4-->>MR: zhipuClient实例

    Note over MR: Master使用最聪明的模型
    MR->>MR: masterModelClient = claudeClient (Sonnet)

    Note over MR: Worker使用快速经济的模型
    MR->>MR: workerModelClient = gptClient (4o-mini)
    Note over MR: 摘要也使用Worker模型

    deactivate MR

    Note over TM,WA: Master执行时使用Claude

    TM->>MA: 模块2::MasterRunner::run(masterSessionId)
    activate MA

    MA->>MR: 模块5::ModelRouter::routeByRole("MASTER")
    MR-->>MA: 返回claudeClient

    MA->>LC1: 模块5::LlmClient::chat(context, systemPrompt, tools)
    activate LC1
    LC1->>LC1: 调用Claude API
    LC1-->>MA: 返回UnifiedMessage
    deactivate LC1

    Note over MA: Master继续执行...

    deactivate MA

    Note over TM,LC4: Worker执行时使用GPT

    MA->>TM: 模块2::TaskManager::createWorkerTask
    TM-->>MA: 返回workerSessionId
    MA->>WA: 模块2::WorkerRunner::run(workerSessionId)
    activate WA

    WA->>MR: 模块5::ModelRouter::routeByRole("WORKER")
    MR-->>WA: 返回gptClient

    WA->>LC2: 模块5::LlmClient::chat(context, systemPrompt, tools)
    activate LC2
    LC2->>LC2: 调用GPT API
    LC2-->>WA: 返回UnifiedMessage
    deactivate LC2

    Note over WA: Worker继续执行...

    deactivate WA

    Note over WA,LC2: 上下文压缩时也使用Worker模型(GPT)

    WA->>MR: 模块5::ModelRouter::getSummarizer()
    MR-->>WA: 返回gptClient

    WA->>LC2: 模块5::LlmClient::summarize(longText)
    activate LC2
    LC2->>LC2: 调用GPT API进行摘要
    LC2-->>WA: 返回精简摘要
    deactivate LC2

    Note over MR: ✅ 配置完成：系统已就绪运行
    Note over MR: 模型配置在启动时设定，运行时固定
```

---

## 配置方式

**启动时配置示例（application.yml）：**

```yaml
yesboss:
  llm:
    # Master 使用的模型（用于规划、决策）
    master:
      provider: claude
      model: claude-3-5-sonnet-20241022
      api-key: ${ANTHROPIC_API_KEY}

    # Worker 使用的模型（用于执行、摘要）
    worker:
      provider: openai
      model: gpt-4o-mini
      api-key: ${OPENAI_API_KEY}
```

**设计优势：**
1. ✅ **简单稳定**：避免运行时配置变更导致的意外行为
2. ✅ **易于测试**：配置固定，测试场景可预测
3. ✅ **成本可控**：通过配置文件控制模型使用，避免意外切换到昂贵模型

**如需更换模型：** 修改配置文件后重启系统即可
