# 3.3 人机回环 (Human-in-the-loop)

## 功能描述
执行过程中系统默认静默运转。但当总/子Agent遇到敏感操作（触发黑名单）时，系统通过5步完美闭环实现"拦截-挂起-求助-决策-缝合"的全流程控制。

## 5步闭环概览

1. **底层拦截与异常抛出 (Intercept)** - SandboxInterceptor 拦截黑名单工具，抛出携带 toolCallId 的异常
2. **状态机挂起 (Suspend)** - SuspendResumeEngine 切换状态、持久化现场、Worker线程完全退出
3. **跨层求助与物理触达 (Spoof & Push)** - 越过 Master LLM，在全局流插入系统消息，推送 IM 卡片
4. **人类决断与路由回调 (Human Decision)** - 用户点击按钮，WebhookController 接收回调并路由
5. **结果落定与记忆缝合 (Execute/Forge & Resume)** - 根据用户决策执行真实工具或伪造错误，缝合上下文并唤醒 Worker

## 时序图

```mermaid
sequenceDiagram
    autonumber

    participant User as 用户
    participant IM as IM系统
    participant WC as WebhookController
    participant GSM as GlobalStreamManager
    participant LSM as LocalStreamManager
    participant SRE as SuspendResumeEngine
    participant TM as TaskManager
    participant WR as WorkerRunner
    participant SI as SandboxInterceptor
    participant TR as ToolRegistry
    participant IMP as IMMessagePusher
    participant UIR as UICardRenderer

    %% 正常执行流程
    activate WR
    WR->>WR: LLM推理决定调用工具

    Note over WR,SI: 步骤1：底层拦截与异常抛出 (Intercept)

    WR->>TR: 模块4::ToolRegistry::getTool(toolName)
    TR-->>WR: 返回工具实例

    WR->>SI: 模块4::SandboxInterceptor::preCheck(tool, argumentsJson, toolCallId)
    activate SI

    SI->>SI: 检查工具名和参数是否匹配黑名单
    
    %% 提前注销 SI，避免 Mermaid 解析器在 alt/else 分支中重复计数报错
    deactivate SI

    alt 命令安全（不在黑名单）
        SI-->>WR: 校验通过
        WR->>WR: 正常执行工具
        Note over WR: 继续ReAct循环

    else 命令危险（在黑名单中）
        SI-->>WR: 抛出 SuspendExecutionException<br/>（携带toolCallId、toolName、argumentsJson）

        Note over WR,SRE: 步骤2：状态机挂起 (Suspend)

        WR->>SRE: 捕获异常，调用<br/>模块2::SuspendResumeEngine::suspendForApproval<br/>(workerSessionId, interceptedCommand, toolCallId)
        activate SRE

        SRE->>TM: 模块2::TaskManager::transitionState(workerSessionId, SUSPENDED)
        activate TM
        TM-->>SRE: 状态已更新
        deactivate TM

        SRE->>SRE: 持久化拦截现场<br/>（toolCallId、toolName、argumentsJson）

        Note over SRE,IMP: 步骤3：跨层求助与物理触达 (Spoof & Push)

        SRE->>TM: 模块2::TaskManager::getParentSessionId(workerSessionId)
        TM-->>SRE: 返回 masterSessionId

        Note over SRE: 越过Master的LLM，直接伪造系统消息

        SRE->>GSM: 模块3::GlobalStreamManager::appendSystemMessage<br/>(masterSessionId, systemText)
        activate GSM
        Note over GSM: 将拦截通知插入Master的全局流<br/>（非人类输入，也非LLM生成）
        GSM-->>SRE: 消息已插入
        deactivate GSM

        SRE->>UIR: 模块1::UICardRenderer::renderSuspensionCard(interceptedCommand)
        activate UIR
        UIR-->>SRE: 返回交互卡片（授权/拒绝按钮）
        deactivate UIR

        SRE->>IMP: 模块1::IMMessagePusher::pushCardMessage<br/>(imType, imGroupId, card)
        activate IMP
        IMP->>IM: 调用IM API推送卡片并@用户
        deactivate IMP

        deactivate SRE
        deactivate WR

        Note over User: 系统进入SUSPENDED状态<br/>Worker线程已完全退出<br/>无限期等待用户决策

        Note over User,WC: 步骤4：人类决断与路由回调 (Human Decision)

        User->>IM: 点击"授权"或"拒绝"按钮
        IM->>WC: Webhook回调事件
        activate WC

        WC->>WC: 解析用户决策（isApproved、humanFeedback）

        alt 用户同意授权 (isApproved=true)
            Note over WC,SRE: 步骤5A：结果落定（真实执行）

            WC->>SRE: 模块2::SuspendResumeEngine::resume<br/>(workerSessionId, toolCallId, true, humanFeedback)
            activate SRE

            SRE->>SRE: 从数据库恢复拦截现场<br/>（toolName、argumentsJson）

            SRE->>TR: 模块4::ToolRegistry::getTool(toolName)
            TR-->>SRE: 返回工具实例

            SRE->>TR: 模块4::AgentTool::executeWithBypass(argumentsJson)
            Note over TR: 绕过沙箱黑名单检查，直接执行

            alt 工具执行成功
                TR-->>SRE: 返回真实执行结果
                SRE->>LSM: 模块3::LocalStreamManager::appendToolResult<br/>(workerSessionId, toolResultMsg, isError=false)
            else 工具执行失败
                TR-->>SRE: 返回异常信息
                SRE->>LSM: 模块3::LocalStreamManager::appendToolResult<br/>(workerSessionId, errorMsg, isError=true)
            end

            activate LSM
            Note over LSM: 将ToolResult注入Worker的局部流
            deactivate LSM

            SRE->>TM: 模块2::TaskManager::transitionState(workerSessionId, RUNNING)
            SRE->>WR: 模块2::WorkerRunner::run(workerSessionId)
            activate WR

            Note over WR,LSM: 【核心规约】唤醒状态判定
            WR->>LSM: 模块3::LocalStreamManager::fetchContext(workerSessionId)
            LSM-->>WR: 返回局部流历史
            WR->>WR: 模块2::WorkerRunner::isResumingFromSuspension(sessionId)
            Note over WR: 判定为唤醒状态，跳过初始化

            Note over WR: 大模型看到ToolResult，继续ReAct循环

            deactivate SRE
            WC-->>User: 通知"已继续执行"

        else 用户拒绝授权 (isApproved=false)
            Note over WC,SRE: 步骤5B：记忆缝合（伪造错误）

            WC->>SRE: 模块2::SuspendResumeEngine::resume<br/>(workerSessionId, toolCallId, false, humanFeedback)
            activate SRE

            SRE->>SRE: 伪造错误文本<br/>"工具调用失败，安全沙箱已拦截：<br/>用户拒绝授权，没有权限执行此操作。"

            SRE->>LSM: 模块3::LocalStreamManager::appendToolResult<br/>(workerSessionId, forgedErrorMsg, isError=true)
            activate LSM
            Note over LSM: 将伪造的错误ToolResult注入Worker局部流
            deactivate LSM

            SRE->>TM: 模块2::TaskManager::transitionState(workerSessionId, RUNNING)
            SRE->>WR: 模块2::WorkerRunner::run(workerSessionId)
            activate WR

            Note over WR,LSM: 【核心规约】唤醒状态判定
            WR->>LSM: 模块3::LocalStreamManager::fetchContext(workerSessionId)
            LSM-->>WR: 返回局部流历史
            WR->>WR: 模块2::WorkerRunner::isResumingFromSuspension(sessionId)
            Note over WR: 判定为唤醒状态，跳过初始化

            Note over WR: 大模型看到"权限不足"错误，尝试其他非黑名单工具

            deactivate SRE
            WC-->>User: 通知"操作已拒绝"
        end

        deactivate WC
    end

    deactivate WR
```

## 关键设计点说明

### 1. 步骤1：底层拦截 (Intercept)
- **SandboxInterceptor.preCheck()** 在工具执行前进行黑名单校验
- 抛出的异常必须携带 `toolCallId`（因果纽带），用于后续缝合上下文

### 2. 步骤2：状态机挂起 (Suspend)
- 状态从 `RUNNING` 切换为 `SUSPENDED`
- 持久化拦截现场（toolName、argumentsJson），供唤醒时恢复
- **Worker 线程完全退出**：`run()` 方法捕获异常后正常结束，释放所有线程和内存资源
- 系统进入挂起状态，仅依赖数据库持久化状态维持，不占用任何线程资源

### 3. 步骤3：跨层求助 (Spoof & Push)
- **越过 Master 的 LLM**：直接调用 `GlobalStreamManager.appendSystemMessage()` 插入系统消息
- **Master 仅作为中介**：不参与决策，只负责消息转发
- **主动推送**：通过 `IMMessagePusher` 将卡片推送到群聊

### 4. 步骤4：人类决策 (Human Decision)
- WebhookController 接收 IM 回调
- 解析用户决策（授权/拒绝/补充指令）
- 路由到 `SuspendResumeEngine.resume()`

### 5. 步骤5：结果落定与记忆缝合 (Execute/Forge & Resume)
- **同意时**：调用 `AgentTool.executeWithBypass()` 绕过沙箱，返回真实结果
- **拒绝时**：伪造"权限不足"错误，不执行任何底层操作
- **ToolResult 缝合**：注入 `LocalStreamManager`，通过 `toolCallId` 关联
- **状态保持 RUNNING**：不立即终止，给模型探索替代方案的机会
- **唤醒 Worker**：创建**新线程**重新调用 `WorkerRunner.run(sessionId)`
- **【核心规约】唤醒状态判定**：
  - 新线程必须先调用 `isResumingFromSuspension(sessionId)` 检查是否为唤醒状态
  - 检查 LocalStream 中最后一条消息是否为 ToolResult
  - 如果是，则跳过初始化（不调用 InjectionEngine），直接基于上下文继续 ReAct 循环
  - 这避免了重复初始化导致的死循环问题

## 状态转换图

```
正常执行: RUNNING
    ↓
触发黑名单拦截
    ↓
    SUSPENDED (挂起，等待用户决策)
    ↓
用户决策
    ↓
┌───────────┴───────────┐
↓                       ↓
同意                   拒绝
↓                       ↓
执行真实工具          伪造错误ToolResult
↓                       ↓
    └───────────┬───────────┘
                ↓
            RUNNING (继续ReAct循环)
```

## 与其他组件的协作

- **模块1（接入与路由）**：WebhookController 接收用户决策，IMMessagePusher 推送卡片
- **模块2（状态机与调度）**：TaskManager 管理状态转换，SuspendResumeEngine 协调整个流程，WorkerRunner 实现唤醒状态判定
- **模块3（双流上下文）**：GlobalStreamManager 跨 Agent 伪造消息，LocalStreamManager 注入 ToolResult
- **模块4（工具链与沙箱）**：SandboxInterceptor 拦截危险操作，AgentTool 提供 bypass 执行模式
