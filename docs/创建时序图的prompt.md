请在项目中建立一个名为 时序图 的目录。请遍历“功能需求文档”中的每一个功能条目，为每一个功能独立生成一个 .md 格式 的时序图文件。

核心约束（严格执行）：

1 组件排他性限制：时序图中的参与对象（Participants/                                                                                                                                 ==>Lifelines）仅允许且必须包含6个详细设计文档中定义的核心组件，每个详细设计文档其实对应一个模块。严禁出现任何未在设计文档中定义的第三方组件、外部模块或临时对象。

2 文件命名规范：每个功能对应一个独立的 Markdown 文件。文件名必须与功能需求名称完全一致（例如：功能名称A.md）。

3 接口调用规范（三段式格式）：时序图上的所有消息传递必须映射到组件的已有接口。方法调用必须严格遵守格式：[模块名]::[接口名]::[方法名]。

4 技术实现：文件内容必须使用 Mermaid 语法编写 sequenceDiagram 代码块，确保可直接渲染。                                                                                            
5 对于需求文档中的每一点功能都必须生成一个md文档来对应，如果该功能无法画用例图也需要一个空的md文件。

输出要求：请逐一输出每个文件的文件名及对应的 Mermaid 代码内容，确保覆盖功能需求文档中的所有 Functional Requirements。 